<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="com.study.nbnb.dao.B1Dao">

	<select id="listDao" resultType="com.study.nbnb.dto.B1Dto">
		select * from b1dao order by id desc
	</select>
	
	<select id="viewDao" resultType="com.study.nbnb.dto.B1Dto">
		select * from b1dao where id = #{0}
	</select>
	
	<insert id="writeDao">
		insert into b1dao (id, writer, title, content)
		values (b1dao_seq.nextval, #{param1},#{param2},#{param3})
	</insert>
	
	<delete id="deleteDao">
		delete from b1dao where id = #{param1}
	</delete>
</mapper>